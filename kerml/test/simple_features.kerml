package Features {
    abstract feature person; // Default subsets Base::things.
    feature A;
//    member feature g featured by A;
//    feature child redefines person;
//    feature A;
//	feature a subsets A;
}